org.gradle.jvmargs=-Xmx4G -Djava.net.preferIPv4Stack=true
android.useAndroidX=true
android.enableJetifier=true
android.defaults.buildfeatures.buildconfig=true
android.nonTransitiveRClass=false
android.nonFinalResIds=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Keep original resource names in release builds: plugins that resolve
# resources by string name at runtime (e.g. flutter_local_notifications
# looking up the "app_icon" drawable) break when AGP shortens resource paths.
android.enableResourceOptimizations=false
